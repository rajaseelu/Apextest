public with sharing class Test1 {
    public static void Addto( Integer a , integer b) {
     integer sum = a+b;
     Integer diff = a-b;//aneel Raja
        System.debug('The two numbers:'+ sum);
    }
}